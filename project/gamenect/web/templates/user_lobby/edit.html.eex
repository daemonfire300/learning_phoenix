<h2>Edit user lobby</h2>

<%= render "form.html", changeset: @changeset,
                        action: user_lobby_path(@conn, :update, @user_lobby) %>

<%= link "Back", to: user_lobby_path(@conn, :index) %>
